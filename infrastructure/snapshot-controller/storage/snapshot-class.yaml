apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshotClass
metadata:
  name: longhorn-snapshot
driver: driver.longhorn.io
deletionPolicy: Delete
parameters:
  # An omitted type means a remote Longhorn backup and therefore requires a
  # configured BackupTarget. This class is intentionally for local snapshots.
  type: snap
