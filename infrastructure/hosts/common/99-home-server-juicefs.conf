# JuiceFS CSI watches its shared configuration with inotify. K3s containers
# share the host root user's inotify-instance quota; 128 was already exhausted
# on the Beelink and close to exhaustion on the Pi.
fs.inotify.max_user_instances = 1024
