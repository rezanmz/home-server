apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: reza-network
  namespace: traefik
spec:
  secretName: reza-network-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - reza.network
    - "*.reza.network"
