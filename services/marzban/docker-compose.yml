services:
  marzban:
    image: gozargah/marzban:latest
    container_name: marzban
    restart: always
    network_mode: host
    
    environment:
      - SQLALCHEMY_DATABASE_URL=sqlite:////var/lib/marzban/db.sqlite3
      - SUDO_USERNAME=admin
      - SUDO_PASSWORD=admin
      
      # 1. Disable Debug to stop the 'npm' crash
      - DEBUG=False
      
      # 2. Force Host Binding
      - UVICORN_HOST=0.0.0.0
      - UVICORN_PORT=8000
      
      # 3. Feed it the Fake SSL certs to unlock 0.0.0.0 binding
      - UVICORN_SSL_CERTFILE=/var/lib/marzban/certs/fullchain.pem
      - UVICORN_SSL_KEYFILE=/var/lib/marzban/certs/key.pem
      
      # ⚠️ NEW: Tell Marzban to accept our self-signed cert
      - UVICORN_SSL_CA_TYPE=private
      
      - XRAY_EXECUTABLE_PATH=/usr/local/bin/xray
      - XRAY_ASSETS_PATH=/usr/local/share/xray
      
    volumes:
      - ~/persistent/marzban:/var/lib/marzban