apiVersion: v1
kind: Service
metadata:
  name: hermes-agent
  namespace: apps
  labels:
    app.kubernetes.io/name: hermes-agent
    app.kubernetes.io/component: application
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hermes-agent
    app.kubernetes.io/component: application
  ports:
    - name: dashboard
      port: 9119
      targetPort: dashboard
      protocol: TCP
