apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-config
  namespace: network-services
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn
