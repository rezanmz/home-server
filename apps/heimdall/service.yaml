apiVersion: v1
kind: Service
metadata:
  name: heimdall
  namespace: apps
  labels:
    app.kubernetes.io/name: heimdall
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: heimdall
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
