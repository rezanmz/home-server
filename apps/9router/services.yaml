apiVersion: v1
kind: Service
metadata:
  name: 9router
  namespace: apps
  labels:
    app.kubernetes.io/name: 9router
    app.kubernetes.io/component: application
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: 9router
    app.kubernetes.io/component: application
  ports:
    - name: http
      port: 20128
      targetPort: http
      protocol: TCP
