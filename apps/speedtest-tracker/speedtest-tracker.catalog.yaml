# yaml-language-server: $schema=../../catalog/service.schema.json
apiVersion: catalog.reza.network/v1alpha1
kind: CatalogExclusion
metadata:
  name: speedtest-tracker
spec:
  reason: Retired workload; only its APP_KEY Secret and config PVC remain reconciled as recovery artifacts pending a verified final export.
