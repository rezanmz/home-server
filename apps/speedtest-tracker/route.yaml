apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: speedtest-tracker
  namespace: apps
spec:
  parentRefs:
    - name: home
      namespace: traefik
  hostnames:
    - speedtest.reza.network
  rules:
    - backendRefs:
        - name: speedtest-tracker
          port: 80
