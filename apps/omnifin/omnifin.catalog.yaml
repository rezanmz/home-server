# yaml-language-server: $schema=../../catalog/service.schema.json
apiVersion: catalog.reza.network/v1alpha1
kind: CatalogExclusion
metadata:
  name: omnifin
spec:
  reason: Retired workload; only its data PVC and encryption/recovery Secret remain reconciled as recovery artifacts. The Authentik OIDC provider, application, and provider-side client secret are retained (no tested cleanup lifecycle).
