apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: {name: sonarr, namespace: media}
spec:
  parentRefs:
    - {name: home, namespace: traefik}
  hostnames: [sonarr.reza.network]
  rules:
    - filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: custom-errors
      backendRefs:
        - {name: sonarr-access, port: 80}
