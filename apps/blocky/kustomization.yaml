apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - deployment.yaml
  - metrics.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: blocky-config
    namespace: network-services
    files:
      - config.yml
