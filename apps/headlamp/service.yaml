apiVersion: v1
kind: Service
metadata:
  name: headlamp
  namespace: monitoring
  labels: {app.kubernetes.io/name: headlamp}
spec:
  selector: {app.kubernetes.io/name: headlamp}
  ports:
    - {name: http, port: 80, targetPort: access-http}
    - {name: metrics, port: 9090, targetPort: http}
