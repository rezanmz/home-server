# yaml-language-server: $schema=../../catalog/service.schema.json
apiVersion: catalog.reza.network/v1alpha1
kind: CatalogExclusion
metadata:
  name: maintainerr-tmdb-proxy
spec:
  reason: Internal allowlisted egress proxy with no browser-facing service or external hostname.
