apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argilla
  namespace: apps
spec:
  parentRefs:
    - name: home
      namespace: traefik
  hostnames:
    - argilla.reza.network
  rules:
    - backendRefs:
        - name: argilla
          port: 6900
