apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Argilla retired after a verified additive merge into the GCP instance.
  # Reconcile only frozen rollback state; runtime manifests remain unreferenced
  # in this directory for recovery analysis.
  - secrets.sops.yaml
  - pvc.yaml
