apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mcphub
  namespace: apps
spec:
  parentRefs:
    - name: home
      namespace: traefik
  hostnames:
    - mcphub.reza.network
  rules:
    - backendRefs:
        - name: mcphub
          port: 3000
