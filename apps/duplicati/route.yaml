apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: {name: duplicati, namespace: network-services}
spec:
  parentRefs:
    - {name: home, namespace: traefik}
  hostnames: [duplicati.reza.network]
  rules:
    - backendRefs:
        - {name: duplicati-access, port: 80}
