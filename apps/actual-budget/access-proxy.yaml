apiVersion: v1
kind: ConfigMap
metadata:
  name: actual-budget-access-proxy
  namespace: apps
data:
  nginx.conf: |
    pid /tmp/nginx.pid;
    events {}
    http {
      access_log /dev/stdout;
      error_log /dev/stderr warn;
      map $http_upgrade $connection_upgrade { default upgrade; '' close; }
      set_real_ip_from 10.42.0.0/16;
      real_ip_header X-Forwarded-For;
      real_ip_recursive on;
      server {
        listen 15006;
        # Actual's encrypted sync uploads may legitimately approach its own
        # 50 MiB application limit. Keep a finite proxy ceiling above that
        # limit so nginx does not reject ordinary sync payloads at its 1 MiB
        # default while Actual remains the endpoint-specific size boundary.
        client_max_body_size 64m;
        allow 192.168.1.0/24;
        allow 10.8.0.0/24;
        allow 10.42.1.0/24; # wg-easy masqueraded clients on the Pi
        deny all;
        location / {
          proxy_pass http://127.0.0.1:5006;
          proxy_http_version 1.1;
          proxy_set_header Host $http_host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection $connection_upgrade;
        }
      }
    }
