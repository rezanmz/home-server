apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: wg-easy-lan-only
  namespace: network-services
spec:
  ipAllowList:
    sourceRange:
      - 192.168.1.0/24
      - 10.8.0.0/24
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: wg-easy
  namespace: network-services
spec:
  parentRefs:
    - name: home
      namespace: traefik
  hostnames:
    - vpn.reza.network
  rules:
    - filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: custom-errors
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: wg-easy-lan-only
      backendRefs:
        - name: wg-easy
          port: 51821
