apiVersion: v1
kind: Service
metadata:
  name: finance-display
  namespace: apps
  labels:
    app.kubernetes.io/name: finance-display
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: finance-display
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
