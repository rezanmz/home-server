apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: finance-display-state
  namespace: apps
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
