apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: {name: glances, namespace: monitoring}
spec:
  parentRefs:
    - {name: home, namespace: traefik}
  hostnames: [glances.reza.network]
  rules:
    - backendRefs:
        - {name: glances, port: 80}
