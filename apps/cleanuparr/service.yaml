apiVersion: v1
kind: Service
metadata:
  name: cleanuparr
  namespace: media
  labels:
    app.kubernetes.io/name: cleanuparr
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cleanuparr
    app.kubernetes.io/component: download-safety
  ports:
    - name: http
      port: 11011
      targetPort: http
      protocol: TCP
