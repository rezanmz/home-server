apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: {name: jellyfin, namespace: media}
spec:
  parentRefs:
    - {name: home, namespace: traefik}
  hostnames: [jellyfin.reza.network]
  rules:
    - backendRefs:
        - {name: jellyfin-access, port: 80}
