{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "dependencyDashboard": true,
  "packageRules": [
    {
      "description": "All LinuxServer.io images (docker.io/linuxserver/* and lscr.io/linuxserver/*) use non-SemVer compound tag formats. A single regex matches both registries automatically.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["/linuxserver\//"],
      "versioning": "loose"
    },
    {
      "description": "linuxserver/qbittorrent has a stale Ubuntu-era CalVer tag '20.04.1' that sorts higher than the real 5.x app-version tags. Exclude it.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["linuxserver/qbittorrent"],
      "allowedVersions": "!/^20\\.04\\.1$/"
    },
    {
      "description": "servercontainers/samba uses a custom composite tag format (e.g. a3.23.4-s4.22.8-r0). Loose versioning prevents misidentification.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["ghcr.io/servercontainers/samba"],
      "versioning": "loose"
    },
    {
      "description": "pgvector uses channel tags like 'pg16' — not version numbers. Disable updates entirely to prevent bogus PRs.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["pgvector/pgvector"],
      "enabled": false
    },
    {
      "description": "Images pinned to a build-variant suffix (e.g. '-full') must only update within that same variant. Detected automatically via matchCurrentValue — no package list needed.",
      "matchDatasources": ["docker"],
      "matchCurrentValue": "/-full$/",
      "allowedVersions": "/-full$/"
    },
    {
      "description": "Group all non-major Docker updates into a single PR to reduce noise.",
      "matchDatasources": ["docker"],
      "matchUpdateTypes": ["minor", "patch", "digest"],
      "groupName": "Docker non-major updates",
      "minimumReleaseAge": "1 day"
    },
    {
      "description": "Major Docker updates always get individual PRs for manual review.",
      "matchDatasources": ["docker"],
      "matchUpdateTypes": ["major"],
      "automerge": false
    }
  ]
}
